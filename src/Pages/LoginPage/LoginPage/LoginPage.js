import React from 'react';
import Login from '../Login/Login';
import './LoginPage.css';

const LoginPage = () => {
    return (
        <div>
            <Login />
        </div>
    );
};

export default LoginPage;